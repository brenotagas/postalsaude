import { OcultarCompetenciaPipe } from './ocultar-competencia.pipe';

describe('OcultarCompetenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarCompetenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
